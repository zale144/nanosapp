syntax = "proto3";

package account;

service AccountService {
    rpc Get(Account) returns (Account) {}
    rpc Add(Account) returns (Account) {}
}

message Account {
    string username = 1;
    string password = 2;
}
